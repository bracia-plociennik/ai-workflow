# Human-Facing Templates

Templates for documents intended for owners, operators, or non-agent readers.

Use these only when the artifact is meant for `docs/human/<project>/...`. Workflow execution artifacts belong in `docs/projects/<project>/...`.
