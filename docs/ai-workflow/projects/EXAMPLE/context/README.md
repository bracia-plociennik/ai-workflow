# Context

## Purpose

This directory stores raw/supporting project context materials for the EXAMPLE workspace.

## Belongs Here

- Raw/supporting inputs for the accepted project context in `../context.md`.
- Raw briefs, specifications, brand notes, logos, images, PDFs, client guidelines, product notes, and other project-specific source material before idea validation.
- References that help architecture, planning, specifications, and QA understand the project scope.

Before `../context.md` is accepted, this directory can contain raw owner/client input. `phase-0-idea-validation` must review these materials instead of relying only on chat messages.

Supporting source files in this directory are exempt from `scripts/ai-workflow/check-naming` and may keep client/source filenames. The accepted project context is not exempt: it must be the canonical `../context.md` file before architecture and later phases.

## Does Not Belong Here

- Repo-wide context; use `docs/ai-workflow/repo/context.md` and `docs/ai-workflow/repo/context/`.
- Phase evidence; use `quality/`.
- Task specifications; use `specs/`.
- Human-facing summaries; use `docs/ai-workflow/humans/EXAMPLE/`.

## Current Artifacts

- `../context.md`
