# <Project> Memory Router

## Purpose

`docs/ai-workflow/projects/<project>/memory.md` is the router and index for project memory.

Detailed entries live in `docs/ai-workflow/projects/<project>/memory/`.

It does not override repository state, `AGENTS.md`, `docs/ai-workflow/ai/workflow.md`, `docs/ai-workflow/ai/workflow/`, or approved phase/task artifacts.

## Memory Index

| Date | Topic | Type | Status | Route |
| --- | --- | --- | --- | --- |
| n/a | No project memory entries yet | n/a | n/a | n/a |

## Rules

- Keep this file short. It is an index, not the memory body.
- Store detailed project memory entries in `docs/ai-workflow/projects/<project>/memory/`.
- Do not store repo-level, external workflow, or template maintenance memory here.
- Do not store secrets, credentials, customer data, or production-only operational details here.
- Use project memory only after evidence-backed distillation/checkpoint work.
