# Human-Facing Templates

Templates for documents intended for owners, operators, or non-agent readers.

Use these only when the artifact is meant for `docs/ai-workflow/humans/<project>/...`. Workflow execution artifacts belong in `docs/ai-workflow/projects/<project>/...`.
