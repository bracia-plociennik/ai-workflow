# AI System Project Workspace

## Purpose

This project prepared a local Mac AI workspace variant of AI Workflow.

The project has now been moved out of this `ai-workflow` repository and should continue in a separate `ai-system` repository.

## Current Route

- Project workspace: historical.
- Idea validation: accepted with changes.
- Architecture: PASS after QA.
- Project plan: PASS after QA.
- Specification package: PASS after QA.
- Implementation package: phase 5 quality PASS before transfer.
- Current state: transferred to separate repository; no further phases run here.

## Runtime Rules

- Do not implement system changes from this workspace.
- Do not recreate or maintain an `ai-system` branch in this repository unless the owner explicitly reopens the migration decision.
- Future `ai-system` implementation work belongs in the separate repository.
- Keep customer data and secrets out of `.systems/`.
- Treat `ai-system-workspace/` as local runtime for the future local system.
