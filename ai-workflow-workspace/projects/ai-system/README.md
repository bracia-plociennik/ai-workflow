# AI System Project Workspace

## Purpose

This project prepares a local Mac AI workspace variant of AI Workflow.

The project is planned inside the current `ai-workflow` repository as workflow runtime only. Implementation is intentionally stopped before `phase-4-implementation`.

## Current Route

- Project workspace: ready.
- Idea validation: accepted with changes.
- Architecture: PASS after QA.
- Project plan: PASS after QA.
- Specification package: PASS after QA.
- Next owner-controlled phase: `phase-4-implementation`.

## Runtime Rules

- Do not implement system changes from this workspace.
- Do not create the final `ai-system` branch before final owner-approved release work.
- Keep customer data and secrets out of `.systems/`.
- Treat `ai-system-workspace/` as local runtime for the future local system.

