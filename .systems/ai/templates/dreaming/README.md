# Dreaming Templates

Templates for advisory-only Dreaming Mode reports.

Dreaming Mode creates recommendation artifacts under `AI_WORKFLOW_WORKSPACE_HOME/dreams/runs/**`. It does not write durable memory, System Insights, External Memory, skills, status, source code, commits, or scheduler automation.
