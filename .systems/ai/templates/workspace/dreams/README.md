# Dreams Workspace Templates

Templates used by `.systems/scripts/init-workspace` and `.systems/scripts/update-workspace` to create `AI_WORKFLOW_WORKSPACE_HOME/dreams/`.

Dream Reports are advisory-only and belong under `AI_WORKFLOW_WORKSPACE_HOME/dreams/runs/**`.
