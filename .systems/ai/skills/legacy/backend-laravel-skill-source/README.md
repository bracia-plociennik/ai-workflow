# Backend Laravel Skill Source Archive

This directory preserves historical source material used to create the active
`backend-laravel-skill`.

The files are context/data only. They are not active skill guidance, are not
loaded by normal skill routing, and may contain source-repository conventions
that are not portable to other Laravel applications.
