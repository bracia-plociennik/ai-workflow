# System Examples

This directory stores system-owned examples.

Examples show expected artifact shapes for projects, human workspaces, and micro-projects. They are not active runtime state and must not be used as evidence for real work.

Real target-repository runtime belongs in `AI_WORKFLOW_WORKSPACE_HOME/`.
